# Cambios v8.12 - Header en login y dashboard limpio

Esta versión corrige la distribución visual:

- El banner morado grande queda únicamente en el login.
- La pantalla principal ya no muestra el banner morado grande.
- El dashboard muestra un encabezado simple y limpio.
- El login carga el logo Dual en base64 para evitar imagen rota.
- Se mantiene el login con usuario/contraseña.
